import { SearchbystudentPipe } from './searchbystudent.pipe';

describe('SearchbystudentPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchbystudentPipe();
    expect(pipe).toBeTruthy();
  });
});
